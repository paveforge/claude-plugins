# Conventions

Rules that apply to every repo, whatever the language.

Loaded for every task. Language-specific rules go in `<language>.md` and
service-specific rules in `<service>.md`, which wins on conflict.

Keep this file about how code is written. Rules of your own - what agents
should and should not do - belong in the hub's `AGENTS.md` or `CLAUDE.md`,
which is given to every agent alongside these files.

These files describe how the code already is, and `/pave:analyse` drafts them
from your repos. The hub file is what you are telling the agents to do. Where
the two disagree about how code is written, the more specific file wins -
`<service>.md` over `<language>.md` over this one.

## Naming
<...>

## Error handling
<...>

## Testing
<...>

## Logging
<...>
